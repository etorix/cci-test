version: 2
jobs:
  build:
    docker:
      - image: circleci/ruby:latest
      - image: circleci/mongo:latest
        command: [--port 3001]

    steps:
      - run:
         name: test
         shell: /bin/bash -o pipefail
         command: |
                  true
                  true
                  false
                  true
